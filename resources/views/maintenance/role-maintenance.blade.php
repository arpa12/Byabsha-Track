<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ __('app.maintenance_title') }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            margin: 0;
            display: flex;























































</html></body>    </div>        </div>            </form>                <button type="submit" class="btn btn-outline-light btn-sm">{{ __('app.logout') }}</button>                @csrf            <form action="{{ route('logout') }}" method="POST">        <div class="actions">        <p class="mb-0 text-secondary" style="color:#94a3b8 !important;">{{ __('app.maintenance_message') }}</p>        <h1 class="h3 mb-3">{{ __('app.maintenance_heading') }}</h1>        <div class="status-pill">● {{ __('app.maintenance_badge') }}</div>    <div class="maintenance-card"><body></head>    </style>        }            flex-wrap: wrap;            gap: 0.75rem;            justify-content: center;            display: flex;            margin-top: 1.5rem;        .actions {        }            margin-bottom: 1rem;            font-weight: 600;            font-size: 0.82rem;            color: #fcd34d;            border: 1px solid rgba(245, 158, 11, 0.35);            background: rgba(245, 158, 11, 0.15);            border-radius: 999px;            padding: 0.3rem 0.8rem;            gap: 0.45rem;            align-items: center;            display: inline-flex;        .status-pill {        }            text-align: center;            box-shadow: 0 20px 50px rgba(2, 6, 23, 0.5);            padding: 2rem;            border-radius: 16px;            border: 1px solid rgba(148, 163, 184, 0.25);            background: rgba(15, 23, 42, 0.88);            max-width: 560px;            width: 100%;        .maintenance-card {        }            padding: 1rem;            color: #e2e8f0;            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;            background: radial-gradient(circle at top right, #1e3a8a 0%, #0f172a 45%, #020617 100%);            justify-content: center;            align-items: center;
